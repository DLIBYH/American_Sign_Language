{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"gpu","dataSources":[{"sourceType":"datasetVersion","sourceId":29550}],"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# This Python 3 environment comes with many helpful analytics libraries installed\n# It is defined by the kaggle/python Docker image: https://github.com/kaggle/docker-python\n# For example, here's several helpful packages to load\n\nimport numpy as np # linear algebra\nimport pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)\n\n# Input data files are available in the read-only \"../input/\" directory\n# For example, running this (by clicking run or pressing Shift+Enter) will list all files under the input directory\n\nimport os\nfor dirname, _, filenames in os.walk('/kaggle/input'):\n    for filename in filenames:\n        print(os.path.join(dirname, filename))\n\n# You can write up to 20GB to the current directory (/kaggle/working/) that gets preserved as output when you create a version using \"Save & Run All\" \n# You can also write temporary files to /kaggle/temp/, but they won't be saved outside of the current session\n\n# Use the kagglehub client library to attach Kaggle resources like competitions, datasets, and models to your session\n# Learn more about kagglehub: https://github.com/Kaggle/kagglehub/blob/main/README.md\n\nimport kagglehub\n# kagglehub.dataset_download('<owner>/<dataset-slug>')","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:52:05.588789Z","iopub.execute_input":"2026-08-31T18:52:05.589585Z","iopub.status.idle":"2026-08-31T18:53:10.820163Z","shell.execute_reply.started":"2026-08-31T18:52:05.589553Z","shell.execute_reply":"2026-08-31T18:53:10.819226Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"!pip install -q split_folders","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:53:51.120431Z","iopub.execute_input":"2026-08-31T18:53:51.12099Z","iopub.status.idle":"2026-08-31T18:53:55.403852Z","shell.execute_reply.started":"2026-08-31T18:53:51.120958Z","shell.execute_reply":"2026-08-31T18:53:55.402641Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import os\nimport shutil\nimport time\nimport random\nimport json\n\nimport cv2\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\nimport tensorflow as tf\nimport keras\n\nfrom tensorflow.keras import layers\nfrom tensorflow.keras.models import Model, Sequential\nfrom tensorflow.keras.layers import (Dense, Dropout, Flatten, ZeroPadding2D, Conv2D, MaxPooling2D, Activation, GlobalAveragePooling2D, BatchNormalization, DepthwiseConv2D)\nfrom tensorflow.keras.optimizers import Adam\nfrom tensorflow.keras.callbacks import EarlyStopping, ReduceLROnPlateau\nfrom tensorflow.keras.preprocessing.image import ImageDataGenerator\nfrom tensorflow.keras.utils import plot_model\nfrom tensorflow.keras.applications import ( DenseNet201, MobileNetV2, EfficientNetB0)\nfrom tensorflow.keras.applications.densenet import preprocess_input as densenet_preprocess\nfrom tensorflow.keras.applications.mobilenet_v2 import preprocess_input as mobilenet_preprocess\nfrom sklearn.metrics import ( classification_report, accuracy_score, confusion_matrix)\nimport splitfolders","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:54:13.869847Z","iopub.execute_input":"2026-08-31T18:54:13.87018Z","iopub.status.idle":"2026-08-31T18:54:13.892601Z","shell.execute_reply.started":"2026-08-31T18:54:13.87015Z","shell.execute_reply":"2026-08-31T18:54:13.891795Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Reproducibility","metadata":{}},{"cell_type":"code","source":"SEED = 1337\n\nrandom.seed(SEED)\nnp.random.seed(SEED)\ntf.random.set_seed(SEED)\n\nprint(\"TensorFlow version:\", tf.__version__)\nprint(\"Keras version:\", keras.__version__)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:55:34.108463Z","iopub.execute_input":"2026-08-31T18:55:34.109303Z","iopub.status.idle":"2026-08-31T18:55:34.114217Z","shell.execute_reply.started":"2026-08-31T18:55:34.109267Z","shell.execute_reply":"2026-08-31T18:55:34.113457Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"DATASET_ROOT = \"/kaggle/input/datasets/grassknoted/asl-alphabet\"\n\nTRAIN_SOURCE = os.path.join(\n    DATASET_ROOT,\n    \"asl_alphabet_train\",\n    \"asl_alphabet_train\"\n)\n\nTEST_SOURCE = os.path.join(\n    DATASET_ROOT,\n    \"asl_alphabet_test\",\n    \"asl_alphabet_test\"\n)\n\nPREPROCESSED_DATASET = \"/kaggle/working/asl_preprocessed\"\n\nprint(\"Training source:\")\nprint(TRAIN_SOURCE)\n\nprint(\"\\nOfficial test source:\")\nprint(TEST_SOURCE)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:55:52.605552Z","iopub.execute_input":"2026-08-31T18:55:52.606178Z","iopub.status.idle":"2026-08-31T18:55:52.611423Z","shell.execute_reply.started":"2026-08-31T18:55:52.606144Z","shell.execute_reply":"2026-08-31T18:55:52.610513Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class_names = sorted([\n    d for d in os.listdir(TRAIN_SOURCE)\n    if os.path.isdir(\n        os.path.join(TRAIN_SOURCE, d)\n    )\n])\n\nNUM_CLASSES = len(class_names)\n\nprint(\"Number of classes:\", NUM_CLASSES)\nprint(\"\\nClasses:\")\nprint(class_names)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:56:49.922216Z","iopub.execute_input":"2026-08-31T18:56:49.922998Z","iopub.status.idle":"2026-08-31T18:56:49.950591Z","shell.execute_reply.started":"2026-08-31T18:56:49.922964Z","shell.execute_reply":"2026-08-31T18:56:49.949746Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"total_images = 0\n\nclass_counts = {}\n\nfor class_name in class_names:\n\n    class_dir = os.path.join(\n        TRAIN_SOURCE,\n        class_name\n    )\n\n    count = len([\n        f for f in os.listdir(class_dir)\n        if f.lower().endswith(\n            (\".jpg\", \".jpeg\", \".png\")\n        )\n    ])\n\n    class_counts[class_name] = count\n    total_images += count\n\nprint(\"Total training images:\", total_images)\n\nprint(\"\\nImages per class:\")\n\nfor class_name, count in class_counts.items():\n    print(f\"{class_name:10s}: {count}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:57:20.890338Z","iopub.execute_input":"2026-08-31T18:57:20.890611Z","iopub.status.idle":"2026-08-31T18:57:20.9539Z","shell.execute_reply.started":"2026-08-31T18:57:20.890589Z","shell.execute_reply":"2026-08-31T18:57:20.95313Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Preprocessing","metadata":{}},{"cell_type":"code","source":"def preprocess_image(image_path):\n\n    # Read image\n    image = cv2.imread(image_path)\n\n    if image is None:\n        return None\n\n    # 1. Grayscale\n    gray = cv2.cvtColor(\n        image,\n        cv2.COLOR_BGR2GRAY\n    )\n\n    # 2. Gaussian Blur\n    blurred = cv2.GaussianBlur(\n        gray,\n        (5, 5),\n        0\n    )\n\n    # 3. Adaptive Threshold\n    adaptive = cv2.adaptiveThreshold(\n        blurred,\n        255,\n        cv2.ADAPTIVE_THRESH_GAUSSIAN_C,\n        cv2.THRESH_BINARY,\n        11,\n        2\n    )\n\n    # 4. Otsu Threshold\n    _, otsu = cv2.threshold(\n        blurred,\n        0,\n        255,\n        cv2.THRESH_BINARY + cv2.THRESH_OTSU\n    )\n\n    # 5. Combine thresholds\n    #\n    # The thesis describes bitwise inversion\n    # and addition to combine the results.\n    adaptive_inv = cv2.bitwise_not(adaptive)\n\n    combined = cv2.add(\n        adaptive_inv,\n        otsu\n    )\n\n    # Convert result into a clean binary image\n    _, combined = cv2.threshold(\n        combined,\n        127,\n        255,\n        cv2.THRESH_BINARY\n    )\n\n    return combined","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T18:58:13.999777Z","iopub.execute_input":"2026-08-31T18:58:14.000279Z","iopub.status.idle":"2026-08-31T18:58:14.006288Z","shell.execute_reply.started":"2026-08-31T18:58:14.000248Z","shell.execute_reply":"2026-08-31T18:58:14.005348Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Test preprocessing on one image","metadata":{}},{"cell_type":"code","source":"sample_class = class_names[0]\n\nsample_files = [\n    f for f in os.listdir(\n        os.path.join(\n            TRAIN_SOURCE,\n            sample_class\n        )\n    )\n    if f.lower().endswith(\n        (\".jpg\", \".jpeg\", \".png\")\n    )\n]\n\nsample_file = sample_files[0]\n\nsample_path = os.path.join(\n    TRAIN_SOURCE,\n    sample_class,\n    sample_file\n)\n\noriginal = cv2.imread(sample_path)\n\ngray = cv2.cvtColor(\n    original,\n    cv2.COLOR_BGR2GRAY\n)\n\nblurred = cv2.GaussianBlur(\n    gray,\n    (5, 5),\n    0\n)\n\nadaptive = cv2.adaptiveThreshold(\n    blurred,\n    255,\n    cv2.ADAPTIVE_THRESH_GAUSSIAN_C,\n    cv2.THRESH_BINARY,\n    11,\n    2\n)\n\n_, otsu = cv2.threshold(\n    blurred,\n    0,\n    255,\n    cv2.THRESH_BINARY + cv2.THRESH_OTSU\n)\n\nadaptive_inv = cv2.bitwise_not(adaptive)\n\ncombined = cv2.add(\n    adaptive_inv,\n    otsu\n)\n\n_, combined = cv2.threshold(\n    combined,\n    127,\n    255,\n    cv2.THRESH_BINARY\n)\n\nplt.figure(figsize=(20, 5))\n\nplt.subplot(1, 6, 1)\nplt.imshow(\n    cv2.cvtColor(\n        original,\n        cv2.COLOR_BGR2RGB\n    )\n)\nplt.title(\"Original\")\nplt.axis(\"off\")\n\nplt.subplot(1, 6, 2)\nplt.imshow(gray, cmap=\"gray\")\nplt.title(\"Grayscale\")\nplt.axis(\"off\")\n\nplt.subplot(1, 6, 3)\nplt.imshow(blurred, cmap=\"gray\")\nplt.title(\"Gaussian Blur\")\nplt.axis(\"off\")\n\nplt.subplot(1, 6, 4)\nplt.imshow(adaptive, cmap=\"gray\")\nplt.title(\"Adaptive\")\nplt.axis(\"off\")\n\nplt.subplot(1, 6, 5)\nplt.imshow(otsu, cmap=\"gray\")\nplt.title(\"Otsu\")\nplt.axis(\"off\")\n\nplt.subplot(1, 6, 6)\nplt.imshow(combined, cmap=\"gray\")\nplt.title(\"Combined\")\nplt.axis(\"off\")\n\nplt.show()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:01:28.490083Z","iopub.execute_input":"2026-08-31T19:01:28.490573Z","iopub.status.idle":"2026-08-31T19:01:28.814194Z","shell.execute_reply.started":"2026-08-31T19:01:28.49054Z","shell.execute_reply":"2026-08-31T19:01:28.813379Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Preprocess entire dataset","metadata":{}},{"cell_type":"code","source":"if os.path.exists(PREPROCESSED_DATASET):\n    shutil.rmtree(PREPROCESSED_DATASET)\n\nos.makedirs(PREPROCESSED_DATASET)\n\nprint(\"Starting preprocessing...\\n\")\n\nstart_time = time.time()\n\nfor class_name in class_names:\n\n    source_class_dir = os.path.join(\n        TRAIN_SOURCE,\n        class_name\n    )\n\n    target_class_dir = os.path.join(\n        PREPROCESSED_DATASET,\n        class_name\n    )\n\n    os.makedirs(\n        target_class_dir,\n        exist_ok=True\n    )\n\n    files = [\n        f for f in os.listdir(source_class_dir)\n        if f.lower().endswith(\n            (\".jpg\", \".jpeg\", \".png\")\n        )\n    ]\n\n    print(\n        f\"Processing {class_name}: \"\n        f\"{len(files)} images\"\n    )\n\n    for filename in files:\n\n        source_path = os.path.join(\n            source_class_dir,\n            filename\n        )\n\n        target_path = os.path.join(\n            target_class_dir,\n            filename\n        )\n\n        processed = preprocess_image(\n            source_path\n        )\n\n        if processed is not None:\n\n            cv2.imwrite(\n                target_path,\n                processed\n            )\n\nelapsed = time.time() - start_time\n\nprint(\n    f\"\\nPreprocessing completed in \"\n    f\"{elapsed / 60:.2f} minutes.\"\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:02:05.053184Z","iopub.execute_input":"2026-08-31T19:02:05.053469Z","iopub.status.idle":"2026-08-31T19:15:42.402479Z","shell.execute_reply.started":"2026-08-31T19:02:05.053445Z","shell.execute_reply":"2026-08-31T19:15:42.40155Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"#Verify preprocessed dataset\nprocessed_total = 0\n\nfor class_name in class_names:\n\n    class_dir = os.path.join(\n        PREPROCESSED_DATASET,\n        class_name\n    )\n\n    count = len([\n        f for f in os.listdir(class_dir)\n        if f.lower().endswith(\n            (\".jpg\", \".jpeg\", \".png\")\n        )\n    ])\n\n    processed_total += count\n\nprint(\n    \"Original images:\",\n    total_images\n)\n\nprint(\n    \"Preprocessed images:\",\n    processed_total\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:15:42.404294Z","iopub.execute_input":"2026-08-31T19:15:42.404623Z","iopub.status.idle":"2026-08-31T19:15:42.471653Z","shell.execute_reply.started":"2026-08-31T19:15:42.404591Z","shell.execute_reply":"2026-08-31T19:15:42.470905Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"SPLIT_OUTPUT = \"/kaggle/working/asl_split\"\n\nif os.path.exists(SPLIT_OUTPUT):\n    shutil.rmtree(SPLIT_OUTPUT)\n\nsplitfolders.ratio(\n    PREPROCESSED_DATASET,\n    output=SPLIT_OUTPUT,\n    seed=SEED,\n    ratio=(0.70, 0.15, 0.15),\n    group_prefix=None\n)\n\nprint(\"Dataset split completed.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:16.332164Z","iopub.execute_input":"2026-08-31T19:16:16.332575Z","iopub.status.idle":"2026-08-31T19:16:29.081013Z","shell.execute_reply.started":"2026-08-31T19:16:16.332547Z","shell.execute_reply":"2026-08-31T19:16:29.08026Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Define split paths","metadata":{}},{"cell_type":"code","source":"TRAIN_DIR = os.path.join(\n    SPLIT_OUTPUT,\n    \"train\"\n)\n\nVAL_DIR = os.path.join(\n    SPLIT_OUTPUT,\n    \"val\"\n)\n\nTEST_DIR = os.path.join(\n    SPLIT_OUTPUT,\n    \"test\"\n)\n\nprint(TRAIN_DIR)\nprint(VAL_DIR)\nprint(TEST_DIR)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:29.082179Z","iopub.execute_input":"2026-08-31T19:16:29.082504Z","iopub.status.idle":"2026-08-31T19:16:29.087595Z","shell.execute_reply.started":"2026-08-31T19:16:29.08248Z","shell.execute_reply":"2026-08-31T19:16:29.086735Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Count split images","metadata":{}},{"cell_type":"code","source":"def count_dataset_images(directory):\n\n    total = 0\n\n    for class_name in class_names:\n\n        class_dir = os.path.join(\n            directory,\n            class_name\n        )\n\n        if os.path.exists(class_dir):\n\n            total += len([\n                f for f in os.listdir(class_dir)\n                if f.lower().endswith(\n                    (\".jpg\", \".jpeg\", \".png\")\n                )\n            ])\n\n    return total\n\n\nprint(\n    \"Training images:\",\n    count_dataset_images(TRAIN_DIR)\n)\n\nprint(\n    \"Validation images:\",\n    count_dataset_images(VAL_DIR)\n)\n\nprint(\n    \"Test images:\",\n    count_dataset_images(TEST_DIR)\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:38.203654Z","iopub.execute_input":"2026-08-31T19:16:38.204265Z","iopub.status.idle":"2026-08-31T19:16:38.264874Z","shell.execute_reply.started":"2026-08-31T19:16:38.204233Z","shell.execute_reply":"2026-08-31T19:16:38.264005Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Common model configuration","metadata":{}},{"cell_type":"code","source":"INPUT_SHAPE = (224, 224, 3)\nBATCH_SIZE = 24\nEPOCHS = 25\n\nLEARNING_RATE = 0.0001\n\nprint(\"Input shape:\", INPUT_SHAPE)\nprint(\"Batch size:\", BATCH_SIZE)\nprint(\"Epochs:\", EPOCHS)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:41.822573Z","iopub.execute_input":"2026-08-31T19:16:41.823024Z","iopub.status.idle":"2026-08-31T19:16:41.828985Z","shell.execute_reply.started":"2026-08-31T19:16:41.822991Z","shell.execute_reply":"2026-08-31T19:16:41.828041Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Custom CNN data generators","metadata":{}},{"cell_type":"code","source":"custom_train_datagen = ImageDataGenerator(\n    rescale=1.0 / 255.0\n)\n\ncustom_val_datagen = ImageDataGenerator(\n    rescale=1.0 / 255.0\n)\n\ncustom_test_datagen = ImageDataGenerator(\n    rescale=1.0 / 255.0\n)\n\ncustom_train_generator = custom_train_datagen.flow_from_directory(\n    TRAIN_DIR,\n    target_size=(224, 224),\n    color_mode=\"rgb\",\n    batch_size=BATCH_SIZE,\n    class_mode=\"categorical\",\n    shuffle=True,\n    seed=SEED\n)\n\ncustom_val_generator = custom_val_datagen.flow_from_directory(\n    VAL_DIR,\n    target_size=(224, 224),\n    color_mode=\"rgb\",\n    batch_size=BATCH_SIZE,\n    class_mode=\"categorical\",\n    shuffle=False\n)\n\ncustom_test_generator = custom_test_datagen.flow_from_directory(\n    TEST_DIR,\n    target_size=(224, 224),\n    color_mode=\"rgb\",\n    batch_size=BATCH_SIZE,\n    class_mode=\"categorical\",\n    shuffle=False\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:44.7932Z","iopub.execute_input":"2026-08-31T19:16:44.793618Z","iopub.status.idle":"2026-08-31T19:16:46.09201Z","shell.execute_reply.started":"2026-08-31T19:16:44.793589Z","shell.execute_reply":"2026-08-31T19:16:46.09111Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class_indices = custom_train_generator.class_indices\n\nprint(class_indices)\n\nindex_to_class = {\n    v: k\n    for k, v in class_indices.items()\n}\n\nprint(\"\\nIndex to class:\")\nprint(index_to_class)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:50.665413Z","iopub.execute_input":"2026-08-31T19:16:50.666058Z","iopub.status.idle":"2026-08-31T19:16:50.671071Z","shell.execute_reply.started":"2026-08-31T19:16:50.666022Z","shell.execute_reply":"2026-08-31T19:16:50.670323Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Define Custom CNN","metadata":{}},{"cell_type":"code","source":"main_model = keras.models.Sequential([\n\n    layers.Conv2D(\n        32,\n        (3, 3),\n        activation=\"relu\",\n        input_shape=INPUT_SHAPE\n    ),\n\n    layers.DepthwiseConv2D(\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.BatchNormalization(),\n\n    layers.MaxPooling2D(\n        (2, 2)\n    ),\n\n\n    layers.Conv2D(\n        32,\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.DepthwiseConv2D(\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.BatchNormalization(),\n\n    layers.MaxPooling2D(\n        (2, 2)\n    ),\n\n\n    layers.Conv2D(\n        32,\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.DepthwiseConv2D(\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.BatchNormalization(),\n\n    layers.MaxPooling2D(\n        (2, 2)\n    ),\n\n\n    layers.Conv2D(\n        32,\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.DepthwiseConv2D(\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.BatchNormalization(),\n\n    layers.MaxPooling2D(\n        (2, 2)\n    ),\n\n\n    layers.Conv2D(\n        32,\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.DepthwiseConv2D(\n        (3, 3),\n        activation=\"relu\"\n    ),\n\n    layers.BatchNormalization(),\n\n    layers.MaxPooling2D(\n        (2, 2)\n    ),\n\n\n    layers.GlobalAveragePooling2D(),\n\n    layers.Dense(\n        32,\n        activation=\"relu\"\n    ),\n\n    layers.Dense(\n        NUM_CLASSES,\n        activation=\"softmax\"\n    )\n])\n\nmain_model.summary()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:16:54.955643Z","iopub.execute_input":"2026-08-31T19:16:54.956239Z","iopub.status.idle":"2026-08-31T19:16:56.465474Z","shell.execute_reply.started":"2026-08-31T19:16:54.956205Z","shell.execute_reply":"2026-08-31T19:16:56.464617Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"plot_model(\n    main_model,\n    to_file=\"/kaggle/working/custom_cnn.png\",\n    show_shapes=True,\n    show_layer_names=True\n)\n\nprint(\n    \"Model diagram saved to \"\n    \"/kaggle/working/custom_cnn.png\"\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:17:03.442906Z","iopub.execute_input":"2026-08-31T19:17:03.443195Z","iopub.status.idle":"2026-08-31T19:17:04.244996Z","shell.execute_reply.started":"2026-08-31T19:17:03.443171Z","shell.execute_reply":"2026-08-31T19:17:04.244182Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"earlystop_custom = EarlyStopping(\n\nmonitor=\"val_loss\",\npatience=3,\nrestore_best_weights=True\n\n)\n\nlearning_rate_reduction_custom = ReduceLROnPlateau( monitor=\"val_loss\", patience=2, verbose=1, factor=0.1, min_lr=1e-8 )\n\ncustom_callbacks = [ earlystop_custom, learning_rate_reduction_custom ]","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:17:11.752939Z","iopub.execute_input":"2026-08-31T19:17:11.753523Z","iopub.status.idle":"2026-08-31T19:17:11.757914Z","shell.execute_reply.started":"2026-08-31T19:17:11.753495Z","shell.execute_reply":"2026-08-31T19:17:11.757157Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Compile Custom CNN","metadata":{}},{"cell_type":"code","source":"main_model.compile(\n    optimizer=Adam(\n        learning_rate=LEARNING_RATE\n    ),\n    loss=\"categorical_crossentropy\",\n    metrics=[\"accuracy\"]\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:17:22.251519Z","iopub.execute_input":"2026-08-31T19:17:22.251982Z","iopub.status.idle":"2026-08-31T19:17:22.266837Z","shell.execute_reply.started":"2026-08-31T19:17:22.251952Z","shell.execute_reply":"2026-08-31T19:17:22.265967Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Train Custom CNN","metadata":{}},{"cell_type":"code","source":"custom_start_time = time.time()\n\ncustom_history = main_model.fit(\n    custom_train_generator,\n    validation_data=custom_val_generator,\n    callbacks=custom_callbacks,\n    epochs=EPOCHS\n)\n\ncustom_training_time = (\n    time.time() - custom_start_time\n)\n\nprint(\n    f\"\\nCustom CNN training time: \"\n    f\"{custom_training_time / 60:.2f} minutes\"\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T19:17:25.138156Z","iopub.execute_input":"2026-08-31T19:17:25.138546Z","iopub.status.idle":"2026-08-31T20:17:09.626493Z","shell.execute_reply.started":"2026-08-31T19:17:25.138517Z","shell.execute_reply":"2026-08-31T20:17:09.625798Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Training curves","metadata":{}},{"cell_type":"code","source":"plt.figure(figsize=(10, 6))\n\nplt.plot(\n    custom_history.history[\"accuracy\"],\n    label=\"Training Accuracy\"\n)\n\nplt.plot(\n    custom_history.history[\"val_accuracy\"],\n    label=\"Validation Accuracy\"\n)\n\nplt.title(\n    \"Custom CNN Accuracy\"\n)\n\nplt.xlabel(\"Epoch\")\nplt.ylabel(\"Accuracy\")\n\nplt.legend()\n\nplt.show()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:18:54.780763Z","iopub.execute_input":"2026-08-31T20:18:54.781257Z","iopub.status.idle":"2026-08-31T20:18:54.929214Z","shell.execute_reply.started":"2026-08-31T20:18:54.781226Z","shell.execute_reply":"2026-08-31T20:18:54.928409Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"plt.figure(figsize=(10, 6))\n\nplt.plot(\n    custom_history.history[\"loss\"],\n    label=\"Training Loss\"\n)\n\nplt.plot(\n    custom_history.history[\"val_loss\"],\n    label=\"Validation Loss\"\n)\n\nplt.title(\n    \"Custom CNN Loss\"\n)\n\nplt.xlabel(\"Epoch\")\nplt.ylabel(\"Loss\")\n\nplt.legend()\n\nplt.show()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:19:00.785488Z","iopub.execute_input":"2026-08-31T20:19:00.785944Z","iopub.status.idle":"2026-08-31T20:19:00.917007Z","shell.execute_reply.started":"2026-08-31T20:19:00.785915Z","shell.execute_reply":"2026-08-31T20:19:00.916181Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Save Custom CNN","metadata":{}},{"cell_type":"code","source":"main_model.save(\"/kaggle/working/Custom_CNN.keras\")\nprint(\"Custom CNN saved successfully.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:19:05.998377Z","iopub.execute_input":"2026-08-31T20:19:05.998785Z","iopub.status.idle":"2026-08-31T20:19:06.126577Z","shell.execute_reply.started":"2026-08-31T20:19:05.998755Z","shell.execute_reply":"2026-08-31T20:19:06.125723Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"custom_test_generator.reset()\n\ncustom_pred_prob = main_model.predict(\n    custom_test_generator,\n    verbose=1\n)\n\ncustom_pred = np.argmax(\n    custom_pred_prob,\n    axis=1\n)\n\ncustom_true = custom_test_generator.classes\n\nprint(\n    \"Prediction shape:\",\n    custom_pred_prob.shape\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:19:08.66853Z","iopub.execute_input":"2026-08-31T20:19:08.669263Z","iopub.status.idle":"2026-08-31T20:19:23.894305Z","shell.execute_reply.started":"2026-08-31T20:19:08.669232Z","shell.execute_reply":"2026-08-31T20:19:23.893457Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Classification report of Custom CNN","metadata":{}},{"cell_type":"code","source":"print(\n    classification_report(\n        custom_true,\n        custom_pred,\n        target_names=[\n            index_to_class[i]\n            for i in range(NUM_CLASSES)\n        ],\n        digits=4\n    )\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:19:43.456423Z","iopub.execute_input":"2026-08-31T20:19:43.457173Z","iopub.status.idle":"2026-08-31T20:19:43.475262Z","shell.execute_reply.started":"2026-08-31T20:19:43.45714Z","shell.execute_reply":"2026-08-31T20:19:43.474763Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Accuracy","metadata":{}},{"cell_type":"code","source":"custom_accuracy = accuracy_score(\n    custom_true,\n    custom_pred\n)\n\nprint(\n    f\"Custom CNN Test Accuracy: \"\n    f\"{custom_accuracy * 100:.4f}%\"\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:19:58.90656Z","iopub.execute_input":"2026-08-31T20:19:58.906998Z","iopub.status.idle":"2026-08-31T20:19:58.913187Z","shell.execute_reply.started":"2026-08-31T20:19:58.906968Z","shell.execute_reply":"2026-08-31T20:19:58.912335Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Confusion matrix","metadata":{}},{"cell_type":"code","source":"custom_cm = confusion_matrix(\n    custom_true,\n    custom_pred\n)\n\nplt.figure(\n    figsize=(14, 12)\n)\n\nsns.heatmap(\n    custom_cm,\n    annot=True,\n    fmt=\"d\",\n    xticklabels=[\n        index_to_class[i]\n        for i in range(NUM_CLASSES)\n    ],\n    yticklabels=[\n        index_to_class[i]\n        for i in range(NUM_CLASSES)\n    ]\n)\n\nplt.title(\n    \"Custom CNN Confusion Matrix\"\n)\n\nplt.xlabel(\"Predicted\")\nplt.ylabel(\"Actual\")\n\nplt.show()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:20:08.030342Z","iopub.execute_input":"2026-08-31T20:20:08.031024Z","iopub.status.idle":"2026-08-31T20:20:09.534143Z","shell.execute_reply.started":"2026-08-31T20:20:08.030991Z","shell.execute_reply":"2026-08-31T20:20:09.533486Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Per-class accuracy","metadata":{}},{"cell_type":"code","source":"from sklearn.metrics import confusion_matrix\n\ncm = confusion_matrix(custom_true, custom_pred)\n\nclass_accuracy = np.diag(cm) / cm.sum(axis=1)\n\nclass_accuracy_df = pd.DataFrame({\n    \"Class\": classes,\n    \"Accuracy\": class_accuracy * 100\n})\n\nprint(class_accuracy_df)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:26:42.573639Z","iopub.execute_input":"2026-08-31T20:26:42.574087Z","iopub.status.idle":"2026-08-31T20:26:42.58615Z","shell.execute_reply.started":"2026-08-31T20:26:42.574055Z","shell.execute_reply":"2026-08-31T20:26:42.58532Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Top-2 and Top-3 accuracy","metadata":{}},{"cell_type":"code","source":"custom_top2 = np.argsort(\n    custom_pred_prob,\n    axis=1\n)[:, -2:]\n\ncustom_top2_correct = np.array([\n    custom_true[i] in custom_top2[i]\n    for i in range(len(custom_true))\n])\n\ncustom_top2_accuracy = (\n    custom_top2_correct.mean() * 100\n)\n\nprint(\n    f\"Custom CNN Top-2 Accuracy: \"\n    f\"{custom_top2_accuracy:.4f}%\"\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:26:59.112073Z","iopub.execute_input":"2026-08-31T20:26:59.112722Z","iopub.status.idle":"2026-08-31T20:26:59.172Z","shell.execute_reply.started":"2026-08-31T20:26:59.112689Z","shell.execute_reply":"2026-08-31T20:26:59.171398Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"custom_top3 = np.argsort(\n    custom_pred_prob,\n    axis=1\n)[:, -3:]\n\ncustom_top3_correct = np.array([\n    custom_true[i] in custom_top3[i]\n    for i in range(len(custom_true))\n])\n\ncustom_top3_accuracy = (\n    custom_top3_correct.mean() * 100\n)\n\nprint(\n    f\"Custom CNN Top-3 Accuracy: \"\n    f\"{custom_top3_accuracy:.4f}%\"\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:27:03.06479Z","iopub.execute_input":"2026-08-31T20:27:03.065114Z","iopub.status.idle":"2026-08-31T20:27:03.123695Z","shell.execute_reply.started":"2026-08-31T20:27:03.06509Z","shell.execute_reply":"2026-08-31T20:27:03.123117Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Model parameters","metadata":{}},{"cell_type":"code","source":"total_params = main_model.count_params()\n\ntrainable_params = np.sum([\n    np.prod(v.shape)\n    for v in main_model.trainable_weights\n])\n\nnon_trainable_params = np.sum([\n    np.prod(v.shape)\n    for v in main_model.non_trainable_weights\n])\n\nprint(\"Total parameters:\", total_params)\nprint(\"Trainable parameters:\", trainable_params)\nprint(\"Non-trainable parameters:\", non_trainable_params)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:27:07.640291Z","iopub.execute_input":"2026-08-31T20:27:07.641035Z","iopub.status.idle":"2026-08-31T20:27:07.64693Z","shell.execute_reply.started":"2026-08-31T20:27:07.641004Z","shell.execute_reply":"2026-08-31T20:27:07.646245Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Feature maps","metadata":{}},{"cell_type":"code","source":"feature_class = class_names[0]\n\nfeature_files = [\n    f for f in os.listdir(\n        os.path.join(\n            TEST_DIR,\n            feature_class\n        )\n    )\n    if f.lower().endswith(\n        (\".jpg\", \".jpeg\", \".png\")\n    )\n]\n\nfeature_filename = feature_files[0]\n\nfeature_path = os.path.join(\n    TEST_DIR,\n    feature_class,\n    feature_filename\n)\n\nprint(\n    \"Feature map image:\",\n    feature_path\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:27:11.644853Z","iopub.execute_input":"2026-08-31T20:27:11.645233Z","iopub.status.idle":"2026-08-31T20:27:11.651356Z","shell.execute_reply.started":"2026-08-31T20:27:11.645206Z","shell.execute_reply":"2026-08-31T20:27:11.65073Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Display feature maps","metadata":{}},{"cell_type":"code","source":"feature_img = cv2.imread(\n    feature_path,\n    cv2.IMREAD_GRAYSCALE\n)\n\nfeature_img = cv2.resize(\n    feature_img,\n    (224, 224)\n)\n\nfeature_img = (\n    feature_img.astype(\n        np.float32\n    ) / 255.0\n)\n\n# Convert to 3 channels\nfeature_img = np.stack(\n    [feature_img] * 3,\n    axis=-1\n)\n\nfeature_input = np.expand_dims(\n    feature_img,\n    axis=0\n)\n\nprint(\n    \"Feature input shape:\",\n    feature_input.shape\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:27:16.444153Z","iopub.execute_input":"2026-08-31T20:27:16.444941Z","iopub.status.idle":"2026-08-31T20:27:16.453912Z","shell.execute_reply.started":"2026-08-31T20:27:16.444908Z","shell.execute_reply":"2026-08-31T20:27:16.453101Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"custom_feature_model = Model(\n    inputs=main_model.inputs,\n    outputs=main_model.layers[0].output\n)\n\ncustom_feature_maps = (\n    custom_feature_model.predict(\n        feature_input\n    )\n)\n\nprint(\n    \"Custom feature map shape:\",\n    custom_feature_maps.shape\n)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:27:20.209364Z","iopub.execute_input":"2026-08-31T20:27:20.209934Z","iopub.status.idle":"2026-08-31T20:27:20.447243Z","shell.execute_reply.started":"2026-08-31T20:27:20.2099Z","shell.execute_reply":"2026-08-31T20:27:20.446558Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"num_maps = min(\n    custom_feature_maps.shape[-1],\n    32\n)\n\nplt.figure(\n    figsize=(16, 16)\n)\n\nfor i in range(num_maps):\n\n    ax = plt.subplot(\n        4,\n        8,\n        i + 1\n    )\n\n    ax.imshow(\n        custom_feature_maps[0, :, :, i],\n        cmap=\"gray\"\n    )\n\n    ax.set_xticks([])\n    ax.set_yticks([])\n\nplt.suptitle(\n    \"Custom CNN First Conv2D Feature Maps\"\n)\n\nplt.show()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-31T20:27:24.040533Z","iopub.execute_input":"2026-08-31T20:27:24.041301Z","iopub.status.idle":"2026-08-31T20:27:24.830379Z","shell.execute_reply.started":"2026-08-31T20:27:24.041268Z","shell.execute_reply":"2026-08-31T20:27:24.829128Z"}},"outputs":[],"execution_count":null}]}